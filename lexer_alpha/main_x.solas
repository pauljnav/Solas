// Testing Solas v1.1 Non-Compliance

stream @net.ingress
shape UserProfile {
    id: UUID
    token: @env.AUTH_TOKEN
}

refract if latency > 50ms and @env("DEPLOY-STAGE") == "prod"
drift -> @core.static_buffer
emit user.id, @data.archive