// Testing Solas v1.1 Compliance

stream @net.ingress as user
    shape UserProfile
        id: UUID
    on error -> drift to @core.static_buffer

refract UserProfile
    on latency > 50 and @env("DEPLOY_STAGE") == "prod" evolve logic -> "strict"

emit user.id, @data.archive